PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=transitdb
JWT_SECRET=your_super_secret_jwt_key_change_this
JWT_EXPIRES_IN=7d

# Production (Railway) – set this to your MySQL service connection string
# Example: DATABASE_URL=mysql://user:password@host:3306/database
# In Railway, link the MySQL service so this variable is injected automatically.
DATABASE_URL=
